    #include <bits/stdc++.h>
    using namespace std;

    int main()
    {
        set <string> s;
        s.insert("eshaan");    //logn
        s.insert("exyz");
        s.insert("modi");
        // s.find()   only way to access elements of set, returns an iterator
    }
